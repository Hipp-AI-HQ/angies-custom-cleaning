---
phase: 03-residential-site
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - residential/css/residential.css
autonomous: true

must_haves:
  truths:
    - "Opening residential/index.html in browser shows properly styled site with consistent brand colors"
    - "Gift cards section has dark blue gradient background with pricing cards and visible CTAs"
    - "Mobile (375px) shows single-column layouts, desktop (1024px+) shows multi-column grids"
    - "Hero section text is readable with proper contrast on background"
    - "Statistics and services cards match commercial site styling patterns"
  artifacts:
    - path: "residential/css/residential.css"
      provides: "Site-specific styling for residential pages"
      min_lines: 400
      exports: ["residential layouts", "gift-cards section styles"]
  key_links:
    - from: "residential/index.html"
      to: "residential/css/residential.css"
      via: "stylesheet link element"
      pattern: "<link.*residential\\.css"
    - from: "residential/css/residential.css"
      to: "residential/css/variables.css"
      via: "CSS custom properties"
      pattern: "var\\(--[a-z-]+\\)"
---

<objective>
Create residential.css with responsive layouts and gift cards section styling, reusing Phase 2 commercial patterns while adding residential-specific visual enhancements.

Purpose: Provide production-ready CSS that maintains brand consistency with commercial site while adding unique gift cards showcase styling.

Output: 400+ line residential.css covering all sections with mobile-first responsive design and complete gift cards section styling.
</objective>

<execution_context>
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/PROJECT.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/ROADMAP.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/STATE.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/REQUIREMENTS.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/phases/03-residential-site/03-RESEARCH.md

<!-- HTML structure from Plan 02 -->
@/Users/dillonhippensteel/clients/angies-custom-cleaning/residential/index.html

<!-- Phase 2 CSS as styling template -->
@/Users/dillonhippensteel/clients/angies-custom-cleaning/commercial/css/commercial.css
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/phases/02-commercial-site/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create residential.css with all section styles</name>
  <files>residential/css/residential.css</files>
  <action>
    Create residential/css/residential.css by duplicating commercial/css/commercial.css structure and adding gift cards section styling:

    **File structure (same as commercial.css):**
    ```css
    /* ============================================
       Site Header
       ============================================ */
    /* Sticky header, logo, navigation, phone link - copy verbatim from commercial.css */

    /* ============================================
       Hero Section
       ============================================ */
    /* Hero background, headline sizing, CTAs, trust badges - copy verbatim */

    /* ============================================
       Services Section
       ============================================ */
    /* Service cards grid, hover effects - copy verbatim */

    /* ============================================
       Why Choose Us Section
       ============================================ */
    /* Benefits grid - copy verbatim */

    /* ============================================
       How It Works Section
       ============================================ */
    /* Process steps - copy verbatim */

    /* ============================================
       Statistics Section
       ============================================ */
    /* Stats grid, number styling - copy verbatim */

    /* ============================================
       Testimonials Section
       ============================================ */
    /* Testimonial cards - copy verbatim */

    /* ============================================
       Gift Cards Section (NEW)
       ============================================ */
    .gift-cards {
      background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary) 100%);
      color: var(--color-text-on-dark);
    }

    .gift-cards .section__title,
    .gift-cards .section__intro {
      color: var(--color-text-on-dark);
      text-align: center;
    }

    .gift-cards .section__intro {
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
      font-size: var(--fs-lg);
      margin-bottom: var(--space-3xl);
    }

    /* Gift card designs showcase */
    .gift-card-designs {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: var(--space-xl);
      flex-wrap: wrap;
      margin: var(--space-2xl) 0;
    }

    .gift-card {
      width: 300px;
      height: 180px;
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-lg);
      overflow: hidden;
      transition: transform var(--transition-base), box-shadow var(--transition-base);
    }

    .gift-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px rgba(0, 0, 0, 0.3);
    }

    .gift-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    /* Pricing tiers */
    .gift-card-pricing {
      margin: var(--space-3xl) 0;
    }

    .gift-card-pricing h3 {
      color: var(--color-text-on-dark);
      text-align: center;
      margin-bottom: var(--space-xl);
    }

    .pricing-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: var(--space-xl);
      background-color: rgba(255, 255, 255, 0.1);
      border: 2px solid rgba(255, 255, 255, 0.2);
      border-radius: var(--radius-lg);
      transition: all var(--transition-base);
      min-height: 120px;
    }

    .pricing-card:hover {
      background-color: rgba(255, 255, 255, 0.15);
      border-color: var(--color-accent);
      transform: translateY(-2px);
    }

    .pricing-card .price {
      font-size: var(--fs-2xl);
      font-weight: 800;
      color: var(--color-accent);
      font-family: var(--font-heading);
      margin-bottom: var(--space-xs);
    }

    .pricing-card .label {
      font-size: var(--fs-sm);
      color: var(--color-text-on-dark);
      text-align: center;
    }

    /* Purchase CTA */
    .gift-card-cta {
      text-align: center;
      margin: var(--space-3xl) 0 var(--space-2xl);
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
    }

    .gift-card-cta h3 {
      color: var(--color-text-on-dark);
      margin-bottom: var(--space-md);
    }

    .gift-card-cta p {
      font-size: var(--fs-lg);
      margin-bottom: var(--space-xl);
    }

    .gift-card-cta a[href^="tel"] {
      color: var(--color-accent);
      text-decoration: none;
      font-weight: 600;
    }

    .gift-card-cta a[href^="tel"]:hover {
      text-decoration: underline;
    }

    /* Gift card terms */
    .gift-card-terms {
      text-align: center;
      font-size: var(--fs-sm);
      color: rgba(255, 255, 255, 0.9);
      max-width: 600px;
      margin: 0 auto;
    }

    /* Responsive: Stack gift cards on mobile */
    @media (max-width: 768px) {
      .gift-card-designs {
        flex-direction: column;
        gap: var(--space-lg);
      }

      .gift-card {
        width: 100%;
        max-width: 300px;
      }
    }

    /* ============================================
       About Section
       ============================================ */
    /* About content - copy verbatim from commercial.css */

    /* ============================================
       Contact Section
       ============================================ */
    /* Contact form and info card - copy verbatim from commercial.css */

    /* ============================================
       Footer
       ============================================ */
    /* Footer layout and links - copy verbatim from commercial.css */
    ```

    **Key customizations from commercial.css:**
    - Keep ALL existing styles (header, hero, services, why-us, how-it-works, stats, testimonials, about, contact, footer)
    - Add complete gift cards section styling as shown above
    - Maintain all responsive breakpoints (768px, 1024px)
    - Maintain all color usage via CSS custom properties (var(--color-*))
    - Maintain all spacing via spacing scale (var(--space-*))

    **DO NOT:**
    - Change any color values (use variables only)
    - Remove any existing commercial.css patterns
    - Add new fonts or typography scales
    - Break mobile-first responsive flow

    The resulting CSS should be ~400-500 lines (commercial.css ~350 lines + gift cards section ~50-100 lines).
  </action>
  <verify>
    wc -l residential/css/residential.css  # Should be 400+ lines
    grep "gift-cards" residential/css/residential.css  # Verify gift cards styles exist
    grep "var(--color-" residential/css/residential.css | head -5  # Verify using CSS variables
    grep "@media" residential/css/residential.css  # Verify responsive breakpoints exist
  </verify>
  <done>
    residential.css exists with 400+ lines including all commercial site patterns plus complete gift cards section styling using CSS custom properties.
  </done>
</task>

</tasks>

<verification>
Run all task verification commands:
- residential.css has 400+ lines
- Gift cards section styles present
- CSS custom properties used throughout
- Responsive media queries included
</verification>

<success_criteria>
Plan complete when:
- residential/css/residential.css exists with complete site styling
- Gift cards section has blue gradient background, pricing cards, and CTAs styled
- All styles use CSS custom properties from variables.css
- Responsive breakpoints maintain mobile-first design
- Styling patterns match commercial site (consistent brand)
</success_criteria>

<output>
After completion, create `.planning/phases/03-residential-site/03-03-SUMMARY.md` documenting:
- CSS approach (duplicate + extend commercial.css)
- Gift cards section styling decisions
- Responsive design patterns maintained
- Color and spacing token usage
- Files created and line count
</output>
