---
phase: 02-commercial-site
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - commercial/css/commercial.css
autonomous: true

must_haves:
  truths:
    - "Opening commercial/index.html in a browser shows a visually polished site with proper spacing, colors, and typography from the design system"
    - "The header is sticky (stays visible on scroll) and has proper background color with shadow"
    - "Service cards display in a responsive grid (1 column mobile, 2 tablet, 3 desktop)"
    - "All sections have appropriate padding and visual hierarchy"
    - "Trust badges are properly sized and aligned"
    - "Contact form is styled with proper spacing and focus states"
    - "Phone number in header is styled as a tappable button on mobile (48px min height)"
  artifacts:
    - path: "commercial/css/commercial.css"
      provides: "Site-specific styles for all sections and components"
      min_lines: 200
      contains: ".site-header"
  key_links:
    - from: "commercial/css/commercial.css"
      to: "commercial/css/variables.css"
      via: "CSS custom properties"
      pattern: "var\\(--color-|var\\(--space-|var\\(--font-"
    - from: "commercial/css/commercial.css"
      to: "commercial/index.html"
      via: "class selectors"
      pattern: "\\.site-header|\\.hero|\\.service-card"
---

<objective>
Create comprehensive CSS styles for the commercial cleaning site using the design system variables from Phase 1. Style all sections from header to footer with mobile-first responsive layouts, proper spacing, visual hierarchy, and interactive states. Implement sticky header, responsive grids, and tap-friendly mobile elements.

Purpose: This plan transforms the unstyled HTML structure from Plan 02 into a polished, professional-looking site that matches the warm blue brand established in Phase 1. All styles use design system variables (colors, spacing, typography) for consistency with future residential site.

Output: A complete commercial.css stylesheet that styles all page sections responsively across mobile, tablet, and desktop breakpoints.
</objective>

<execution_context>
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/PROJECT.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/ROADMAP.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/STATE.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/.planning/phases/02-commercial-site/02-RESEARCH.md
@/Users/dillonhippensteel/clients/angies-custom-cleaning/CLAUDE.md

# Reference design system variables established in Phase 1
@/Users/dillonhippensteel/clients/angies-custom-cleaning/commercial/css/variables.css

# Reference HTML structure created in Plan 02
@/Users/dillonhippensteel/clients/angies-custom-cleaning/commercial/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style header, hero, and navigation</name>
  <files>
    commercial/css/commercial.css
  </files>
  <action>
    Create commercial/css/commercial.css and begin with header and hero styles. Follow mobile-first approach.

    **Sticky Header (Pattern 2 from 02-RESEARCH.md):**
    ```css
    .site-header {
      position: sticky;
      top: 0;
      z-index: 1000;
      background-color: var(--color-surface);
      box-shadow: var(--shadow-sm);
      padding: var(--space-md) 0;
    }

    .site-header .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: var(--space-lg);
    }

    .logo img {
      height: 48px;
      width: auto;
    }

    nav ul {
      display: none; /* Hidden on mobile, hamburger in Phase 4 */
      list-style: none;
      display: flex;
      gap: var(--space-lg);
    }

    nav a {
      color: var(--color-text);
      text-decoration: none;
      font-weight: 600;
      transition: color var(--transition-base);
    }

    nav a:hover {
      color: var(--color-primary);
    }

    /* Tap-to-call phone link (Pattern 5 from 02-RESEARCH.md) */
    .phone-link {
      display: inline-flex;
      align-items: center;
      gap: var(--space-xs);
      min-height: 48px;
      padding: var(--space-sm) var(--space-md);
      background-color: var(--color-accent);
      color: var(--color-white);
      border-radius: var(--radius-md);
      font-weight: 600;
      text-decoration: none;
      transition: background-color var(--transition-base);
    }

    .phone-link:hover {
      background-color: var(--color-accent-dark);
    }

    @media (min-width: 768px) {
      nav ul {
        display: flex; /* Show nav on tablet+ */
      }

      .phone-link {
        background-color: transparent;
        color: var(--color-primary);
        padding: var(--space-xs) var(--space-sm);
      }
    }
    ```

    **Hero Section:**
    ```css
    .hero {
      background: linear-gradient(135deg, var(--color-primary-light) 0%, var(--color-primary) 100%);
      color: var(--color-white);
      text-align: center;
      padding: var(--space-4xl) 0;
    }

    .hero h1 {
      font-size: var(--font-size-3xl);
      font-weight: 800;
      margin-bottom: var(--space-md);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .hero__subtitle {
      font-size: var(--font-size-lg);
      margin-bottom: var(--space-2xl);
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
    }

    .trust-badges {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: var(--space-lg);
      flex-wrap: wrap;
      margin-top: var(--space-xl);
    }

    .trust-badges img {
      max-width: 80px;
      height: auto;
      filter: brightness(0) invert(1); /* White icons on dark blue bg */
    }

    @media (min-width: 1024px) {
      .hero h1 {
        font-size: var(--font-size-4xl);
      }

      .trust-badges img {
        max-width: 120px;
      }
    }
    ```

    **Buttons:**
    ```css
    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: var(--space-md) var(--space-xl);
      font-size: var(--font-size-base);
      font-weight: 600;
      text-decoration: none;
      border-radius: var(--radius-md);
      transition: all var(--transition-base);
      cursor: pointer;
      border: none;
      min-height: 48px;
    }

    .btn--primary {
      background-color: var(--color-accent);
      color: var(--color-white);
    }

    .btn--primary:hover {
      background-color: var(--color-accent-dark);
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }

    .btn--large {
      padding: var(--space-lg) var(--space-2xl);
      font-size: var(--font-size-lg);
    }
    ```

    Use all design system variables (--color-, --space-, --font-, --radius-, --shadow-, --transition-) from variables.css. Do NOT hardcode values.
  </action>
  <verify>
    Open commercial/index.html in browser. Header should be sticky (stays at top on scroll), logo visible, phone button prominent on mobile. Hero should have blue gradient background, white text, centered content, visible CTA button. Trust badges should be white icons on blue background.
  </verify>
  <done>
    commercial.css contains styles for header (sticky, responsive), navigation, phone link (mobile-optimized tap target), hero section (gradient, centered), trust badges, and button components. All styles use design system variables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Style services, why-us, statistics, and responsive grids</name>
  <files>
    commercial/css/commercial.css
  </files>
  <action>
    Continue commercial.css with section layouts and component styles.

    **Section Base Styles:**
    ```css
    .section {
      padding: var(--space-4xl) 0;
    }

    .section--alt {
      background-color: var(--color-background-alt);
    }

    .section--dark {
      background-color: var(--color-primary);
      color: var(--color-white);
    }

    .section__title {
      font-size: var(--font-size-2xl);
      font-weight: 800;
      text-align: center;
      margin-bottom: var(--space-2xl);
      color: var(--color-primary);
    }

    .section--dark .section__title {
      color: var(--color-white);
    }

    /* Screen reader only utility */
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
    ```

    **Responsive Grid (Pattern 4 from 02-RESEARCH.md):**
    ```css
    .grid {
      display: grid;
      gap: var(--space-xl);
    }

    .grid--3 {
      grid-template-columns: 1fr; /* Mobile: 1 column */
    }

    .grid--2 {
      grid-template-columns: 1fr; /* Mobile: 1 column */
    }

    @media (min-width: 768px) {
      .grid--3 {
        grid-template-columns: repeat(2, 1fr); /* Tablet: 2 columns */
      }

      .grid--2 {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (min-width: 1024px) {
      .grid--3 {
        grid-template-columns: repeat(3, 1fr); /* Desktop: 3 columns */
      }
    }
    ```

    **Card Styles:**
    ```css
    .card {
      background-color: var(--color-surface);
      border-radius: var(--radius-lg);
      padding: var(--space-xl);
      box-shadow: var(--shadow-sm);
      transition: all var(--transition-base);
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-lg);
    }

    .service-card {
      text-align: center;
    }

    .service-card__icon {
      width: 48px;
      height: 48px;
      margin: 0 auto var(--space-lg);
      color: var(--color-primary);
    }

    .service-card h3 {
      font-size: var(--font-size-xl);
      font-weight: 700;
      margin-bottom: var(--space-sm);
      color: var(--color-primary);
    }

    .service-card p {
      color: var(--color-text-light);
    }
    ```

    **Statistics Section:**
    ```css
    .stats__grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: var(--space-2xl);
      text-align: center;
    }

    .stat__number {
      display: block;
      font-size: var(--font-size-4xl);
      font-weight: 800;
      color: var(--color-accent);
      margin-bottom: var(--space-sm);
    }

    .stat__label {
      display: block;
      font-size: var(--font-size-base);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      opacity: 0.9;
    }
    ```

    Ensure all responsive breakpoints match Phase 1 conventions (768px tablet, 1024px desktop). Use mobile-first media queries.
  </action>
  <verify>
    Open commercial/index.html in browser and test responsive behavior. Resize window from 375px (mobile) to 768px (tablet) to 1024px+ (desktop). Services grid should go from 1 col -> 2 col -> 3 col. Statistics should wrap gracefully. Cards should have subtle hover lift effect on desktop.
  </verify>
  <done>
    commercial.css contains responsive section layouts, grid systems (2-col and 3-col), card components with hover states, and statistics styling. All styles are mobile-first and use design system variables.
  </done>
</task>

<task type="auto">
  <name>Task 3: Style testimonials, about, contact form, and footer</name>
  <files>
    commercial/css/commercial.css
  </files>
  <action>
    Complete commercial.css with remaining section styles.

    **Testimonials:**
    ```css
    .testimonial {
      background-color: var(--color-surface);
      border-radius: var(--radius-lg);
      padding: var(--space-xl);
      box-shadow: var(--shadow-sm);
      font-style: italic;
    }

    .testimonial p {
      font-size: var(--font-size-lg);
      line-height: 1.6;
      margin-bottom: var(--space-md);
      color: var(--color-text);
    }

    .testimonial footer {
      font-style: normal;
      font-weight: 600;
      color: var(--color-primary);
      font-size: var(--font-size-sm);
    }
    ```

    **About Section:**
    ```css
    .about .container {
      max-width: 800px;
    }

    .about p {
      font-size: var(--font-size-lg);
      line-height: 1.8;
      margin-bottom: var(--space-lg);
      color: var(--color-text);
    }
    ```

    **Contact Section (Form Pattern 6 from 02-RESEARCH.md):**
    ```css
    .contact__content {
      display: grid;
      gap: var(--space-2xl);
    }

    @media (min-width: 768px) {
      .contact__content {
        grid-template-columns: 1fr 2fr;
      }
    }

    .contact__info p {
      margin-bottom: var(--space-md);
      font-size: var(--font-size-base);
    }

    .contact__info strong {
      color: var(--color-primary);
    }

    .contact__info a {
      color: var(--color-primary);
      text-decoration: none;
      font-weight: 600;
    }

    .contact__form {
      display: flex;
      flex-direction: column;
      gap: var(--space-lg);
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: var(--space-xs);
    }

    .form-group label {
      font-weight: 600;
      color: var(--color-text);
      font-size: var(--font-size-sm);
    }

    .form-group input,
    .form-group textarea {
      padding: var(--space-md);
      border: 2px solid var(--color-border);
      border-radius: var(--radius-md);
      font-size: var(--font-size-base);
      font-family: var(--font-body);
      transition: border-color var(--transition-base);
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--color-primary);
    }
    ```

    **Footer:**
    ```css
    .site-footer {
      background-color: var(--color-primary-dark);
      color: var(--color-white);
      padding: var(--space-2xl) 0;
      text-align: center;
    }

    .site-footer p {
      margin-bottom: var(--space-md);
      opacity: 0.9;
    }

    .site-footer a {
      color: var(--color-white);
      text-decoration: none;
      transition: opacity var(--transition-base);
    }

    .site-footer a:hover {
      opacity: 0.8;
    }
    ```

    **Smooth Scroll and Scroll Margin (Patterns 3 and Pitfall 1 from 02-RESEARCH.md):**
    ```css
    html {
      scroll-behavior: smooth;
    }

    section {
      scroll-margin-top: 80px; /* Account for sticky header height */
    }
    ```

    **Container Utility (Pitfall 7 from 02-RESEARCH.md):**
    ```css
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--space-md);
    }

    @media (min-width: 1024px) {
      .container {
        padding: 0 var(--space-lg);
      }
    }
    ```

    Add any final polish: focus states, active states, print styles if needed. Ensure all images are responsive (max-width: 100%, height: auto).
  </action>
  <verify>
    Open commercial/index.html in browser and test complete page. Check testimonials display properly, about section is readable (max-width 800px), contact form has visible labels and focus states (blue border on focus), footer is dark with white text. Click navigation links to verify smooth scroll behavior. Verify sections don't get hidden behind sticky header when scrolling to anchors.
  </verify>
  <done>
    commercial.css is complete with all section styles (testimonials, about, contact, footer), form accessibility (labels, focus states), smooth scrolling, scroll margin for sticky header offset, and container utilities. File is 200+ lines and ready for production.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Open commercial/index.html in browser at 375px width (mobile)
   - Header should show logo and phone button (no nav links)
   - Phone button should be orange with white text, 48px min height
   - Services grid should be 1 column
2. Resize to 768px (tablet)
   - Navigation links should appear
   - Phone button changes to text link style
   - Services grid should be 2 columns
3. Resize to 1024px+ (desktop)
   - Services grid should be 3 columns
   - All hover effects should work (cards lift, buttons transform)
4. Test navigation links
   - Clicking should scroll smoothly to sections
   - Sections should not be hidden behind sticky header
5. Test form
   - Focus on input should show blue border
   - Labels should be visible above inputs (not just placeholders)
6. Check color consistency
   - All colors should be from design system variables
   - Blue primary, warm white background, orange accent should be consistent throughout
</verification>

<success_criteria>
Plan complete when:
- [ ] commercial.css exists and is 200+ lines
- [ ] Sticky header works (stays at top on scroll)
- [ ] Responsive grids work (1 col mobile, 2 col tablet, 3 col desktop)
- [ ] Phone link is prominent button on mobile (48px min height, orange background)
- [ ] All sections styled with proper spacing and visual hierarchy
- [ ] Form has visible labels and focus states
- [ ] Smooth scrolling works for anchor links
- [ ] Sections don't get hidden behind sticky header (scroll-margin-top applied)
- [ ] All styles use design system variables (no hardcoded colors/spacing)
- [ ] Site looks polished and professional across all breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/02-commercial-site/02-03-SUMMARY.md` documenting CSS patterns used, responsive breakpoints, accessibility considerations, and any design decisions made.
</output>
